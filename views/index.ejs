<!DOCTYPE html>
<html lang="zh-TW">
  <head>
    <title>隨便交易中心</title>
    <meta charset="utf-8">
		<!-- <meta name="viewport" content="width=device-width, initial-scale=1.0"> -->
		<link rel="stylesheet" href="stylesheets/exchange.css">
		<link rel="icon" href="image/favicon.ico" type="image/x-icon">
		<link rel="shortcut icon" href="image/favicon.ico" type="image/x-icon">
  </head>
   <body>
		<div class="wrapper">
			<header>
				<h1>隨便交易中心</h1>

				<div class="nav-left"> 
					<nav>
						<a href="/index">首頁</a>
					</nav>
				</div>

				<div class="nav-right">
					<nav>

						<% if (signed) { %>
						
							<a href="/login">登出</a>
							<a href="/personal_page">個人資料</a>
							<% console.log(signed) %>
						<% }else { %>
							<a href="/register">註冊</a>
							<a href="/login">登入</a>
							<% console.log(signed) %>

						<% } %>
						
					</nav>
				</div>
			</header>

			<main>
				<div class="items">
				<% add.forEach(function(post, index){ %>
					<div class="item">
						<div class="item_title"><a href="">
							<div class="item_name">
								商品名稱: <%= post.post %>
							</div></a>
							<div class="item_owner">
								<br>商品介紹: <%= post.des %>
							</div>
						</div>
	
						<div class="item_time">
							<div class="item_time_start">
								開始日期: <%= post.start %>
							</div>
							<div class="item_time_end">
								結束日期: <%= post.end %>
							</div>
						</div>
	
						<div class="item_price">
							目前售價: <%= post.price %> 
						</div>
						<div class="item_owner">
							<br>得標人為: (得標人)
						</div>
	
					</div>
				<% }) %>

					
				</div>
				<% if (signed) { %>
					<form method="post" action="/post" class="well form-inline center" style="text-align:center;">
						留言: <input type="text" class="span8" name="post">
						<button type="submit" class="btn btn-success"><i class="icon-comment icon-white"></i>發言</button>
					</form>
				<%}%>
				<div class="print">
				<% posts.forEach(function(post, index) { %>

					<h2><a href="/u/<%= post.name %>"><%= post.name %></a> 說</h2>
					<p><%= post.msg %></p>
				  

				<%}) %>
				</div>
			</main>

			<footer>
				<p>
					Copyright &copy; 第二小隊交易系統<br>
					<a href="mailto:session@sitcon.org">Email someone(?</a><br>
				</p>
			</footer>
		</div>

  </body>
</html>
